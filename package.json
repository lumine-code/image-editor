{
  "name": "image-editor",
  "author": "lumine-code",
  "description": "View and edit images directly in the editor.",
  "repository": "https://github.com/lumine-code/image-editor",
  "bugs": {
    "url": "https://github.com/lumine-code/image-editor/issues"
  },
  "keywords": [
    "viewer",
    "png",
    "jpg",
    "svg",
    "crop",
    "rotate",
    "filter"
  ],
  "version": "1.0.0",
  "license": "MIT",
  "engines": {
    "lumine": "^1.0.0"
  },
  "backgroundTips": [
    "In the image editor you can crop the picture to the current selection with {{ 'image-editor:crop-to-selection' | keystroke }}"
  ],
  "main": "./lib/main",
  "files": [
    "assets",
    "docs",
    "keymaps",
    "lib",
    "menus",
    "spec",
    "styles"
  ],
  "scripts": {
    "test": "lumine --test spec",
    "test:unit": "node --test \"test/*.test.js\"",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@lumine-code/etch": "github:lumine-code/etch#df06b3fca2ec49b1d6b76cb8af3e9f5015a2612c"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-n": "^18.3.0",
    "globals": "^17.11.0",
    "prettier": "^3.9.6"
  },
  "providedServices": {
    "image-editor": {
      "versions": {
        "1.0.0": "provideImageEditor"
      }
    },
    "navigation.adapter": {
      "versions": {
        "1.0.0": "provideNavigationAdapter"
      }
    }
  },
  "consumedServices": {
    "status-bar": {
      "versions": {
        "^1.0.0": "consumeStatusBar"
      }
    },
    "tree-view.selection": {
      "versions": {
        "^1.0.0": "consumeTreeViewSelection"
      }
    }
  },
  "deserializers": {
    "ImageEditor": "deserialize"
  },
  "configSchema": {
    "defaultBackgroundColor": {
      "title": "Default Background Color",
      "type": "string",
      "enum": [
        "native",
        "white",
        "black",
        "transparent"
      ],
      "default": "native"
    },
    "autoSelectTolerance": {
      "title": "Auto-Select Tolerance",
      "description": "Color difference tolerance for auto-selection (0-255). Lower values require more similar colors to be considered background.",
      "type": "integer",
      "minimum": 0,
      "maximum": 255,
      "default": 30
    },
    "largeImageThreshold": {
      "title": "Async Decode Threshold",
      "description": "File size (in MB) above which an image is decoded asynchronously as it loads.",
      "type": "number",
      "minimum": 0.5,
      "maximum": 50,
      "default": 2
    },
    "largeImagePixelThreshold": {
      "title": "Large Image Threshold",
      "description": "Size (in megapixels) above which undo states are stored compressed and fewer of them are kept.",
      "type": "number",
      "minimum": 0.25,
      "maximum": 100,
      "default": 4
    },
    "maxHistorySize": {
      "title": "Maximum History Size",
      "description": "Maximum number of undo/redo states to keep (reduced automatically for large images).",
      "type": "integer",
      "minimum": 5,
      "maximum": 200,
      "default": 50
    },
    "wheelNavigationDelay": {
      "title": "Mouse Wheel Navigation Delay",
      "description": "Minimum delay (in milliseconds) between mouse wheel navigation events. Higher values prevent accidental rapid scrolling.",
      "type": "integer",
      "minimum": 10,
      "maximum": 500,
      "default": 50
    },
    "showSuccessMessages": {
      "title": "Show Success Messages",
      "description": "Show success notifications after operations like crop, transform, save, etc.",
      "type": "boolean",
      "default": true
    },
    "switchZoomAndNavigation": {
      "title": "Switch Zoom and Navigation",
      "description": "When enabled, scroll wheel navigates and Ctrl+scroll zooms. When disabled, scroll wheel zooms and Ctrl+scroll navigates.",
      "type": "boolean",
      "default": true
    },
    "autoZoomLimit": {
      "title": "Limit Auto Zoom to 100%",
      "description": "When enabled, auto zoom (fit to viewport) will not scale images beyond 100%. When disabled, small images will be scaled up to fit the viewport.",
      "type": "boolean",
      "default": true
    },
    "scrollCycle": {
      "title": "Cycle Through Images",
      "description": "When enabled, navigating past the last image wraps to the first image (and vice versa). When disabled, navigation stops at the boundaries.",
      "type": "boolean",
      "default": true
    }
  }
}
